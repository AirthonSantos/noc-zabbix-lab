---
- name: Gerenciar serviços Linux
  hosts: all
  become: yes

  vars:
    service_name: "{{ service }}"
    service_action: "{{ desired_state }}"

  tasks:
    - name: Executar ação no serviço
      ansible.builtin.systemd:
        name: "{{ service_name }}"
        state: "{{ service_action }}"